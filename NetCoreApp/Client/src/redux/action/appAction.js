import { SET_SELECTED_SIDE_MENU } from '../../constant/actionTypeEnum';

export const setSelectedSideMenu = (data) => ({
  type: SET_SELECTED_SIDE_MENU,
  data,
});

export default setSelectedSideMenu;
